<%= form_for(@payment_status,:html => {:class=>'form-horizontal', :role => 'form'}) do |f| %>
  <% if @payment_status.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@payment_status.errors.count, "error") %> prohibited this payment_status from being saved:</h4>

      <ul>
      <% @payment_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-xs-4">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>  
    </div>
  </div>
  
  <div class="form-group">
    <div class="col-xs-4">
      <%= f.label :note %>
      <%= f.text_area :note, class: 'form-control' %>
    </div>
  </div>  
     
  <br/>
  <p><%= f.submit class: 'btn btn-primary' %></p>
<% end %>
